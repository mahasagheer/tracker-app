import React from 'react';

export default function TeamFilterDropdown() {
  return (
    <select className="bg-accent border border-accent rounded px-3 py-1 text-sm text-dark focus:outline-none">
      <option>All Teams</option>
    </select>
  );
} 